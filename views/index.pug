extends layout

block content
  div.page
    h1= title
    div#board
      each val in messages
        div#post
          p#dateAndName= `${val.user} ${val.added}`
          p= val.text
    a(href='/new') New message

